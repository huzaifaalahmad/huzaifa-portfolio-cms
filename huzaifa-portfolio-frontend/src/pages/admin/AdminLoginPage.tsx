import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '@/context/AuthContext';
export default function AdminLoginPage(){const{login}=useAuth();const nav=useNavigate();const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);try{await login(String(fd.get('email')),String(fd.get('password')));nav('/admin/dashboard')}catch{setError('Invalid credentials or backend connection failed.')}}return <main className="admin-login"><form onSubmit={submit} className="login-card"><span className="eyebrow">Admin Access</span><h1>Welcome back</h1><p>Manage portfolio content, messages, visibility, order, and featured items.</p><input name="email" type="email" placeholder="admin@example.com" required/><input name="password" type="password" placeholder="Password" required/>{error&&<p className="error">{error}</p>}<button className="btn btn-primary">Login</button></form></main>}
